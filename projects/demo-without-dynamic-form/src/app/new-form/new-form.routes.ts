import { Routes } from '@angular/router';
import { NewFormComponent } from './new-form.component';

export const NEW_FORM_ROUTES: Routes = [
  { path: '', component: NewFormComponent, title: 'New form example' }
]